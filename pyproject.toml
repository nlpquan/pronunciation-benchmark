[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pronunciation-benchmark"
version = "0.1.0"
description = "Open benchmark for TTS pronunciation accuracy on non-Western and multilingual names"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
